[DEFAULT]
# the default branch for upstream sources:
upstream-branch = upstream-experimental
# the default branch for the debian patch:
debian-branch = suites/excalibur-security
# Don't use pristine-tar:
pristine-tar = False

debian-tag = devuan/%(version)s
upstream-tag = xorg-server-%(version)s
